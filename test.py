import numpy as np
import pdb
import matplotlib.pyplot as plt

# allReversal = np.asarray([1, 2, 3, 5, 6])
# currentStim = np.asarray([1, 2, 3, 4, 5, 4, 3, 2, 3, 4, 3])

# mean = np.mean(currentStim[allReversal])

# print('Current Stim Mean: {}'.format(mean))

# temp = np.diff(currentStim, 2)

# print(temp)
# print(np.where(temp != 0)[0] + 1)


# plt.plot(currentStim)
# plt.show()

temp = [True, False, None]